meta {
  name: Send Email (Invalid)
  type: http
  seq: 3
}

post {
  url: http://localhost:8080/api/v1/email/send
  body: json
  auth: none
}

body:json {
  {
    "to": "invalid-email",
    "subject": "Should Fail",
    "templateName": "account_activation",
    "variables": {
      "name": "Buggy"
    }
  }
}
