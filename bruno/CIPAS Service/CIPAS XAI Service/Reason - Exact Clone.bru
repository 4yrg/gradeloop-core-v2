meta {
  name: Reason - Exact Clone
  type: http
  seq: 1
}

post {
  url: {{CIPAS_XAI_URL_V1}}/reason
  body: json
  auth: inherit
}

body:json {
  {
    "type": "TYPE-01",
    "code": [
      "function calculate(n) {\n  let result = 0;\n  for(let i=0; i<n; i++) {\n    result += i;\n  }\n  return result;\n}",
      "function calculate(n) {\n    let result = 0;\n    for (let i = 0; i < n; i++) {\n        result += i;\n    }\n    return result;\n}"
    ]
  }
}
