meta {
  name: Reason - Renamed Clone
  type: http
  seq: 2
}

post {
  url: {{CIPAS_XAI_URL_V1}}/reason
  body: json
  auth: inherit
}

body:json {
  {
    "type": "TYPE-02",
    "code": [
      "function addNumbers(a, b) {\n  const sum = a + b;\n  return sum;\n}",
      "function computeResult(x, y) {\n  const val = x + y;\n  return val;\n}"
    ]
  }
  
}
