meta {
  name: Instructor Override
  type: http
  seq: 4
}

patch {
  url: {{BASE_URL}}/api/v1/evaluations/{{SUBMISSION_ID}}/override
  body: json
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

body:json {
  {
    "adjusted_score": 85,
    "reason": "Student demonstrated excellent understanding despite minor execution issues"
  }
}
