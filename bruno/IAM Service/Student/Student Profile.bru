meta {
  name: Student Profile
  type: http
  seq: 2
}

get {
  url: {{authUrlV1}}/profile
  auth: bearer
}

auth:bearer {
  token: {{student_token}}
}

tests {
  test("Should return 200", function() {
    expect(res.status).to.equal(200);
  });
}