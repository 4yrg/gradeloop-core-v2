meta {
  name: Get Users By IDs
  type: http
  seq: 4
}

post {
  url: {{usersUrlV1}}/bulk
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwtToken}}
}

body:json {
  {
    "ids": ["{{user_id}}"]
  }
}
}}"]
}