meta {
  name: Get Users By IDs
  type: http
  seq: 4
}

post {
  url: {{USERS_URL_V1}}/bulk
  body: json
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

body:json {
  {
    "ids": ["uuid-1", "uuid-2"]
  }
}
