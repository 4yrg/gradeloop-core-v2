meta {
  name: Update User
  type: http
  seq: 4
}

put {
  url: {{usersUrlV1}}/{{user_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwtToken}}
}

body:json {
  {
    "userType": "instructor",
    "isActive": true
  }
}
}