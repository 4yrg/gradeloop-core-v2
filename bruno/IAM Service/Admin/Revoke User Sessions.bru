meta {
  name: Revoke User Sessions
  type: http
  seq: 7
}

post {
  url: {{BASE_URL}}/api/v1/admin/users/:id/revoke-sessions
  body: none
  auth: bearer
}

params:path {
  id: uuid-goes-here
}

auth:bearer {
  token: {{ADMIN_TOKEN}}
}
