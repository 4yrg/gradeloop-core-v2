meta {
  name: Revoke User Sessions
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/v1/admin/users/{{user_id}}/revoke-sessions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwtToken}}
}