meta {
  name: List Users
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/users/
  auth: bearer
}

auth:bearer {
  token: {{jwtToken}}
}

tests {
  test("Should return 200", function() {
    expect(res.status).to.equal(200);
  });
  
  test("Should contain users array", function() {
    expect(res.body.users).to.not.be.null;
  });
}