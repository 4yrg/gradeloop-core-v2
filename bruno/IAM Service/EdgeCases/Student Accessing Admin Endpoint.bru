meta {
  name: Student Accessing Admin Endpoint
  type: http
  seq: 3
}

get {
  url: {{usersUrlV1}}/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{student_token}}
}

tests {
  test("Should return 403", function() {
    expect(res.status).to.equal(403);
  });
}