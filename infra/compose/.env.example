# Environment variables for Docker Compose substitution
# Copy this file to .env and fill in the values from ../../.env

# Database Configuration (required for compose file substitution)
POSTGRES_URL_BASE=postgres://user:password@host:port/
POSTGRES_SSLMODE=require
